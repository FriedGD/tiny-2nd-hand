name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      # Versions are pinned so repeated environment creation is predictable.
      - Flask==3.1.3
      - Flask-SocketIO==5.6.1
      # Flask-WTF 1.3.0 requires Python 3.10 or newer.
      - Flask-WTF==1.2.2
